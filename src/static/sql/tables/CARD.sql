CREATE TABLE IF NOT EXISTS CARD(
    ID CHAR(36) NOT NULL DEFAULT (UUID()),
    CREATED_ON_DATE DATETIME(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CHANGED_ON_DATE DATETIME(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    DECK_ID CHAR(36) NOT NULL,
    CATEGORY ENUM('PROMPT', 'RESPONSE') NOT NULL DEFAULT 'PROMPT',
    TEXT VARCHAR(510) NOT NULL,
    YOUTUBE CHAR(11) NULL,
    IMAGE BLOB NULL,
    PRIMARY KEY(ID),
    FOREIGN KEY(DECK_ID) REFERENCES DECK(ID) ON DELETE CASCADE,
    FULLTEXT KEY(TEXT),
    CONSTRAINT DECK_TEXT_UNIQUE UNIQUE(DECK_ID, TEXT)
);