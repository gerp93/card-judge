-- Add ENABLE_LLM_GRAMMAR_CHECK column to LOBBY table
-- This migration safely adds the column if it doesn't already exist

-- ALTER TABLE LOBBY ADD COLUMN ENABLE_LLM_GRAMMAR_CHECK BOOLEAN NOT NULL DEFAULT TRUE;
